#include<stdio.h>
int main()
{
    char ch;
    printf("Enter a character to see ascii value: ");
    scanf("%c", &ch);

    printf("You entered %c.\n", ch);

    printf("ASCII value is %d.", ch);

    return 0;
}
